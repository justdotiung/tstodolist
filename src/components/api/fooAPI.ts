let data: [];

/*
export const post = (data) => {
  data.push(data);
}

export const get = (id) => {
  return data[id];
}

export const delete = (id) => {
  data.filter(data => data.id !== id);
} 
*/
